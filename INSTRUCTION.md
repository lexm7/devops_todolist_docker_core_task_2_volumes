# Django-Todolist Instructions

This document explains how to run the Django-Todolist application and its MySQL database using Docker.

## Prerequisites
- Docker installed on your machine.
- Docker Hub account (optional for pulling images).

## Running the MySQL Container
1. Pull the MySQL image from Docker Hub:
   ```bash
   docker pull lexm7/mysql-local:1.0.0